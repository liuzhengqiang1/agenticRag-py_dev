# ------------------------------
# Web 框架
# ------------------------------
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0                  # ⭐ 生产级 WSGI 服务器
sse-starlette==1.8.2              # ⭐ 用于 Server-Sent Events 流式响应

# ------------------------------
# LangChain 核心 + 扩展
# ------------------------------
langchain==1.2.14
langchain-classic==1.0.3          # ⭐ 新增包，用于 EnsembleRetriever
langchain-community==0.4.1
langchain-openai==1.1.12
langchain-text-splitters==1.1.1
langchain-tavily==0.2.17
langchain-chroma==1.1.0          # ⭐ Chroma 新版，替代 langchain 内置 Chroma
langgraph==1.1.6            # ⭐ 工业级 Agent 状态机框架

# ------------------------------
# DashScope Embeddings
# ------------------------------
dashscope==1.25.15                  # ⭐ 必须安装，支持 DashScopeEmbeddings

# ------------------------------
# 向量数据库
# ------------------------------
chromadb==1.5.5                  # 最新版，兼容 telemetry 修复
elasticsearch==8.12.0             # ⭐ Elasticsearch Python 客户端

# ------------------------------
# 工具库
# ------------------------------
python-dotenv==1.0.0
pydantic==2.12.5
rank-bm25==0.2.2                  # ⭐ BM25 关键字检索算法
flashrank==0.2.8                  # ⭐ 轻量级重排模型（无需 PyTorch）
tavily-python==0.7.23              # ⭐ Tavily 搜索 API（用于 Agentic RAG）

# ------------------------------
# Redis（用于会话历史存储）
# ------------------------------
redis==7.4.0                      # ⭐ Redis Python 客户端
langgraph-checkpoint-redis==0.4.0
cachetools==5.3.3                 # ⭐ LRU 缓存（用于内存降级时防止 OOM）

# ------------------------------
# 图片处理（用于图片过滤）
# ------------------------------
pillow==12.2.0                    # ⭐ 图片尺寸检测
